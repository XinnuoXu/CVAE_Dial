# CVAE_full_dialogue_generator
In order to predict in interactive mode, using a pretrained model, run the following command:
```
python predictor_coherence_interactive.py best_model/model_best.tar.gz
```
